# =============================================================================
# 社交媒體內容生成系統 - 簡化版依賴清單
# 只包含當前爬蟲功能必要的依賴，後續功能會逐步添加
# =============================================================================

# -----------------------------------------------------------------------------
# 核心 Web 框架
# -----------------------------------------------------------------------------
fastapi==0.104.1
uvicorn[standard]==0.24.0
sse-starlette==1.6.5

# -----------------------------------------------------------------------------
# 數據驗證和配置管理
# -----------------------------------------------------------------------------
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0

# -----------------------------------------------------------------------------
# HTTP 客戶端和爬蟲
# -----------------------------------------------------------------------------
apify-client==1.7.1
httpx==0.25.2
requests==2.31.0

# -----------------------------------------------------------------------------
# 基礎工具
# -----------------------------------------------------------------------------
click==8.1.7
python-dateutil==2.8.2

# -----------------------------------------------------------------------------
# 測試工具（開發用）
# -----------------------------------------------------------------------------
pytest==7.4.3
pytest-asyncio==0.21.1

# -----------------------------------------------------------------------------
# 後續會添加的依賴（註解掉，需要時再啟用）
# -----------------------------------------------------------------------------

# AI 和 LLM（Analysis Agent 需要）
# google-generativeai==0.3.2
# openai==1.3.7

# UI 框架（Streamlit UI 需要）
# streamlit==1.28.1

# 資料庫（數據持久化需要）
# sqlalchemy==2.0.23
# asyncpg==0.29.0
# psycopg2-binary==2.9.9

# 快取和訊息佇列（擴展功能需要）
# redis==5.0.1
# nats-py==2.6.0

# 監控和追蹤（生產環境需要）
# opentelemetry-api==1.21.0
# prometheus-client==0.19.0

# 數據處理（分析功能需要）
# pandas==2.1.4
# numpy==1.25.2

# -----------------------------------------------------------------------------
# 安裝說明
# -----------------------------------------------------------------------------
# 1. 創建虛擬環境：python -m venv venv
# 2. 啟動虛擬環境：
#    - Windows: venv\Scripts\activate
#    - macOS/Linux: source venv/bin/activate
# 3. 安裝依賴：pip install -r requirements.txt
# 4. 後續需要新功能時，取消註解對應依賴並重新安裝